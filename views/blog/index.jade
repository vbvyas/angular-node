p {{posts.length}} posts

div(ng-repeat='post in posts')
  h4 {{post.title}}
  div {{post.text}}
  a(href='/blog/show/{{post.id}}') View
  | -
  a(href='/blog/edit/{{post.id}}') Edit
  | -
  a(href='/blog/delete/{{post.id}}') Delete
